let maxSubArr = (arr) => {
  let maxSum = 0, maxSub = [];
for (let i = 0; i < arr.length; i++) {
  const ele = arr[i];
  // stuff
}
}

let collection = [1, 2, 3, 4, 5, -1, -2, -3]
maxSubArr(collection)